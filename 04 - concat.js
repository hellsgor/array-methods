// concat
// создаёт новый массив, в который копирует данные из других массивов

const array1 = [1, 2, 3, 5, 8];
const array2 = [13, 21, 34];

const newArray1 = array1.concat(99);
console.log(newArray1);

const newArray2 = array1.concat(array2);
console.log(newArray2);

